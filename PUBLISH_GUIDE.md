# Полное руководство по публикации ChatList

Это пошаговое руководство поможет вам опубликовать ChatList на GitHub Release и GitHub Pages.

## 📋 Содержание

1. [Подготовка репозитория](#подготовка-репозитория)
2. [Настройка GitHub Actions](#настройка-github-actions)
3. [Публикация на GitHub Release](#публикация-на-github-release)
4. [Настройка GitHub Pages](#настройка-github-pages)
5. [Обновление лендинга](#обновление-лендинга)

---

## 1. Подготовка репозитория

### Шаг 1.1: Создание репозитория

1. Перейдите на https://github.com/new
2. Имя репозитория: `ChatList`
3. Описание: `Приложение для сравнения ответов нейросетей`
4. Выберите публичный или приватный
5. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)

### Шаг 1.2: Инициализация Git

```powershell
# Если репозиторий еще не инициализирован
git init
git add .
git commit -m "Initial commit: ChatList v1.0.0"

# Добавьте удаленный репозиторий
git remote add origin https://github.com/Drogobar/ChatList.git
git branch -M main
git push -u origin main
```

---

## 2. Настройка GitHub Actions

### Шаг 2.1: Создание Personal Access Token

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. "Generate new token (classic)"
3. Название: `ChatList Release Token`
4. Выберите права:
   - ✅ `repo` (полный доступ)
   - ✅ `workflow` (обновление workflows)
5. "Generate token"
6. **Скопируйте токен** (показывается только один раз!)

### Шаг 2.2: Добавление секрета

1. Репозиторий → Settings → Secrets and variables → Actions
2. "New repository secret"
3. Name: `GITHUB_TOKEN`
4. Secret: вставьте токен из шага 2.1
5. "Add secret"

### Шаг 2.3: Проверка workflows

Убедитесь, что файлы созданы:
- ✅ `.github/workflows/release.yml`
- ✅ `.github/workflows/pages.yml`

---

## 3. Публикация на GitHub Release

### Способ A: Автоматическая публикация (Рекомендуется)

#### Шаг 3.1: Обновление версии

Если нужно изменить версию:

```powershell
# Отредактируйте version.py
# __version__ = "1.0.0"
```

#### Шаг 3.2: Создание тега и релиза

```powershell
# Сделайте commit изменений
git add .
git commit -m "Prepare release v1.0.0"

# Создайте тег
git tag -a v1.0.0 -m "Release version 1.0.0"

# Отправьте тег
git push origin v1.0.0
```

#### Шаг 3.3: GitHub Actions соберет релиз

1. Перейдите в Actions
2. Дождитесь завершения workflow "Build and Release"
3. Перейдите в Releases
4. Релиз будет создан автоматически

#### Шаг 3.4: Редактирование релиза (опционально)

1. Releases → выберите ваш релиз
2. "Edit release"
3. Добавьте описание из `RELEASE_NOTES_TEMPLATE.md`
4. "Update release"

### Способ B: Ручная публикация

Если GitHub Actions не работает:

1. Соберите приложение локально:
   ```powershell
   .\build.bat
   .\build_installer.ps1
   ```

2. Создайте релиз на GitHub:
   - Releases → "Draft a new release"
   - Tag: `v1.0.0`
   - Title: `ChatList v1.0.0`
   - Description: используйте шаблон из `RELEASE_NOTES_TEMPLATE.md`

3. Загрузите файлы:
   - `dist\ChatList-1.0.0.exe`
   - `installer\ChatList-Setup-1.0.0.exe`

4. "Publish release"

---

## 4. Настройка GitHub Pages

### Шаг 4.1: Включение GitHub Pages

1. Репозиторий → Settings → Pages
2. Source: выберите **GitHub Actions**
3. Сохраните

### Шаг 4.2: Первая публикация

```powershell
# Добавьте файлы лендинга
git add docs/
git add .github/workflows/pages.yml
git commit -m "Add GitHub Pages landing page"
git push origin main
```

### Шаг 4.3: Проверка

1. Подождите 1-2 минуты
2. Проверьте Actions → должен быть workflow "Deploy GitHub Pages"
3. Откройте сайт: `https://Drogobar.github.io/ChatList/`

### Шаг 4.4: Обновление ссылок в лендинге

Отредактируйте `docs/index.html`:

1. Обновите ссылки на скачивание (используйте правильные версии)
2. Обновите ссылки на скачивание (используйте правильные версии)
3. Commit и push:
   ```powershell
   git add docs/index.html
   git commit -m "Update landing page links"
   git push origin main
   ```

---

## 5. Обновление лендинга

### Шаг 5.1: Редактирование контента

Отредактируйте файлы в папке `docs/`:
- `docs/index.html` - основной контент
- `docs/assets/css/style.css` - стили

### Шаг 5.2: Публикация изменений

```powershell
git add docs/
git commit -m "Update landing page"
git push origin main
```

GitHub Actions автоматически обновит сайт через 1-2 минуты.

---

## 6. Проверочный список

Перед публикацией убедитесь:

- [ ] Все ссылки в `docs/index.html` обновлены (заменен `Drogobar`)
- [ ] Версия в `version.py` соответствует релизу
- [ ] Версия в `ChatList.iss` соответствует релизу
- [ ] GitHub Actions секрет `GITHUB_TOKEN` добавлен
- [ ] GitHub Pages включен и настроен на GitHub Actions
- [ ] Workflows файлы находятся в `.github/workflows/`
- [ ] Лендинг проверен локально (откройте `docs/index.html` в браузере)

---

## 7. Обновление версии

При выпуске новой версии:

1. Обновите `version.py`:
   ```python
   __version__ = "1.0.1"
   ```

2. Обновите `ChatList.iss`:
   ```iss
   #define MyAppVersion "1.0.1"
   #define MyAppExeName "ChatList-1.0.1.exe"
   ```

3. Создайте новый тег:
   ```powershell
   git add .
   git commit -m "Bump version to 1.0.1"
   git tag -a v1.0.1 -m "Release version 1.0.1"
   git push origin main
   git push origin v1.0.1
   ```

4. GitHub Actions автоматически создаст новый релиз

---

## 8. Troubleshooting

### Проблема: GitHub Actions не запускается

**Решение:**
- Проверьте, что файл `.github/workflows/release.yml` существует
- Убедитесь, что тег создан правильно: `v1.0.0` (с буквой v)
- Проверьте синтаксис YAML файла

### Проблема: Установщик не собирается

**Решение:**
- Убедитесь, что Inno Setup установлен в системе GitHub Actions
- Проверьте пути к файлам в `ChatList.iss`
- Проверьте, что файл `dist/ChatList-*.exe` существует

### Проблема: GitHub Pages не обновляется

**Решение:**
- Проверьте Actions → должен быть успешный workflow
- Убедитесь, что файлы в папке `docs/`
- Подождите 1-2 минуты (кеширование)
- Очистите кеш браузера (Ctrl+F5)

### Проблема: Ссылки на скачивание не работают

**Решение:**
- Проверьте, что файлы загружены в релиз
- Убедитесь, что имена файлов в ссылках соответствуют реальным
- Используйте прямые ссылки: `https://github.com/.../releases/download/v1.0.0/...`

---

## 9. Полезные ссылки

- [GitHub Releases API](https://docs.github.com/en/rest/releases)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Inno Setup Documentation](https://jrsoftware.org/ishelp/)

---

**Готово!** Теперь ваше приложение опубликовано и доступно для скачивания! 🎉
